# Challenge Gate — BUG-001 (security/privacy-class auto-trigger)

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-06-21 · Project: road-rash

BUG-001 auto-triggers the challenge gate (PII/privacy-class finding). Applying the common-sense and
scaffolding tests:

- **Self-documenting placeholder?** No. The value is a live Cognito `email` claim, not a
  `change-me`/`example`/`placeholder` string.
- **Invented "expected behavior"?** No. The expectation (public pages must not expose a user's email)
  is a standard privacy requirement, and the value is rendered on an unauthenticated route.
- **Documented design decision?** No comment or doc sanctions exposing the email; the code comment
  even calls it a "best-effort display name," implying a label, not an identifier.
- **Would it embarrass us to report upstream?** No — it's a legitimate, defensible finding.

**Verdict: CONFIRMED** (Medium). Severity not escalated to High because exposure requires an account
whose token lacks a `name` claim, and the data is a display label rather than a credential. Fixed in
c36ea99.

BUG-002 and BUG-003 were reviewed for the same patterns (no placeholders, no documented sanction,
real contracts) — both CONFIRMED at their stated severities (Med-Low, Low). Neither is security-
credential-class, so the lighter review applies. No findings were downgraded or rejected.
