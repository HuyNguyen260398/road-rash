# Spec Audit Triage — road-rash

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-06-21 · Project: road-rash

## Pre-audit docs validation
No citable formal spec ships (`reference_docs/` absent). `docs/architecture.md`,
`docs/road-rash-plan.md`, and `docs/Project_Architecture_Blueprint.md` are the intent baseline; all
requirements are Tier 3 (code-is-spec). The audit ran against the **patched** tree (BUG-001/002/003
fixes already committed: c36ea99, 78add4b, ac64572), so its job was (a) verify the fixes hold and
(b) hunt for net-new findings.

## Effective council
3/3 auditors returned usable reports (privacy lens, URL/iframe lens, authz/input lens). Each
performed a full pass; the lens only biased attention.

## Consolidated verdicts

| REQ | Auditor(s) | Verdict | Evidence |
|-----|-----------|---------|----------|
| REQ-001 | 1 | SATISFIED (fix verified) | auth.ts:33 — email removed; only producer is createTrip |
| REQ-002 | 2 | SATISFIED (fix verified) | validation.ts:34 charset + :50 encode; both call sites gated |
| REQ-002b | 2 | SATISFIED | validator-gated src + sandbox + CSP frame-src all intact |
| REQ-003 | 3 | SATISFIED (fix verified) | validate.ts:97-99 on both create + update paths |
| REQ-004 | 3 | SATISFIED | 403/401 owner branches; favorites keyed by sub |
| REQ-005 | 3 | SATISFIED | unchanged; 23 functional tests green |
| REQ-006/007/008 | — | SATISFIED | candidate containment, tolerant parse, counter floor |
| REQ-009 | 1 | SATISFIED (accepted) | authorId exposure needed for isOwner; opaque UUID |

## Minority findings
None. No auditor raised a finding the others contradicted.

## Net-new code bugs
**Zero.** The three previously-found bugs are confirmed fixed. The additional subsystems audited this
pass (FavoritesProvider optimistic state, lib/search.ts) contained no defects.

## Carried follow-ups (non-code — not bugs)
1. **BUG-001 data scrub:** existing `authorName` rows that already hold an email predate the fix —
   a one-off DynamoDB scrub is recommended (data migration).
2. **BUG-002 legacy mid caveat:** the charset tightening may reject a pre-existing non-URL-safe `mid`
   (embed shows the fallback link). Unlikely for real My Maps ids; documented.
3. **C4 / C5 boundaries:** case-sensitive server search + unpaginated Scan are accepted launch-scale
   design boundaries (ASSUMPTION-001), to revisit before data growth.

## Layer-2 semantic citation check
N/A — zero Tier 1/2 requirements (Spec-Gap run). `citation_semantic_check.json` written with an empty
`reviews` array per the artifact contract.

## Outcome
Spec audit adds no net-new bugs. All requirements SATISFIED on the patched tree. BUGS.md needs no
new entries.
